<nav class="navbar navbar-expand-lg navbar-light py-0 mb-3">
    <a class="navbar-brand" href="{{ path('index') }}">{{ websitename }}</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item {% if app.request.attributes.get( '_route' ) starts with 'admin_home_' %}active{% endif %}">
                <a class="nav-link" href="{{ path('admin_home_index') }}">Scan</a>
            </li>
            <li class="nav-item {% if app.request.attributes.get( '_route' ) starts with 'admin_product_' %}active{% endif %}">
                <a class="nav-link" href="{{ path('admin_product_index') }}">Producten</a>
            </li>
            <li class="nav-item {% if app.request.attributes.get( '_route' ) starts with 'admin_user_' %}active{% endif %}">
                <a class="nav-link" href="#">Gebruikers</a>
            </li>
        </ul>
        <ul class="navbar-nav ml-auto">
            <span class="navbar-text">Welkom, {{ app.user.username }}</span>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_logout') }}">Uitloggen</a>
            </li>
        </ul>
    </div>
</nav>